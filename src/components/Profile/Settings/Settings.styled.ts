import { styled } from '@mui/material';

export const Card = styled('div')(() => ({
  backgroundColor: '#FFF',
  width: 'calc(100% / 1.5)',
  borderRadius: '8px',
  margin: '0 auto',
  padding: '15px',
  pt: '30px',
}));
